[package]
name = "genrt-arch-aarch64"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[lib]
crate-type = ["staticlib"]

[dependencies]
kernel = { path = "../../kernel" }
bootinfo = { path = "../../crates/bootinfo" }
